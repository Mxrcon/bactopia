config {
    testsDir "."
    workDir System.getenv("NFT_WORKDIR") ?: ".nf-test"
    configFile "../nextflow.config"
    profile ""
    options "--is_ci --max_cpus 4 --max_memory 7.GB"

    plugins {
        load "nft-utils@0.0.5"
    }
}
